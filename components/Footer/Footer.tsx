import Container from '@mui/material/Container';
import styles from './Footer.module.css';

const Footer = () => {
  return (
    <Container fixed className={styles.footer}>
      Esercizio di Edoardo Vincenzi
    </Container>
  );
};

export default Footer;
